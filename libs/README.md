<!-- =============================================================================
 libft/ (opcional)

 O QUE FAZER AQUI:
 - Se a dupla decidir usar a própria libft, colar aqui as fontes (*.c, *.h)
   e o Makefile da libft já pronto de outro projeto.
 - O Makefile principal (../Makefile) deve compilar essa lib primeiro
   (make -C libft) antes de compilar o push_swap, e linkar com -lft.
 - Se não for usar libft, essa pasta pode ficar vazia/ser removida —
   nesse caso, implementar equivalentes simples (ft_atoi próprio, etc.)
   diretamente em src/parsing/.
============================================================================== -->
