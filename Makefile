# =============================================================================
# Makefile — push_swap
#
# O QUE FAZER AQUI:
# - Definir NAME = push_swap
# - Listar todos os .c em src/ (parsing, stack, operations, disorder,
#   algorithms, selector, bench, main.c)
# - CC = cc | CFLAGS = -Wall -Wextra -Werror
# - Regras obrigatórias: $(NAME), all, clean, fclean, re
# - Se for usar libft: compilar a lib via "make -C libft" ANTES de linkar
#   o push_swap, e linkar com -L libft -lft
# - Regra "bonus": compila o checker (arquivos em checker_bonus/) usando
#   headers/libs extras que não são permitidos na parte obrigatória
# - Cuidado para não fazer relink desnecessário (usar dependências de .o
#   corretamente, não recompilar tudo a cada "make")
# =============================================================================
