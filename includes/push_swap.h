/* =============================================================================
 * push_swap.h
 *
 * O QUE FAZER AQUI:
 * - Includes necessários (stdlib.h, unistd.h, limits.h, etc.)
 * - Definir a struct do nó da stack (ex: t_node { int value; t_node *next;
 *   t_node *prev; })
 * - Definir a struct de estado geral do programa (ex: t_ps) contendo:
 *     - ponteiro para topo da stack a
 *     - ponteiro para topo da stack b
 *     - tamanho de cada stack
 *     - contadores de operações (sa, sb, ss, pa, pb, ra, rb, rr, rra, rrb, rrr)
 *     - disorder calculado
 *     - flag/nome da estratégia selecionada
 *   -> Isso evita variáveis globais, que são proibidas no subject.
 * - Protótipos de todas as funções públicas de cada módulo:
 *     parsing/, stack/, operations/, disorder/, algorithms/, selector/, bench/
 * - Defines de enum para as estratégias (SIMPLE, MEDIUM, COMPLEX, ADAPTIVE)
 * ========================================================================== */

#ifndef PUSH_SWAP_H
# define PUSH_SWAP_H

#endif
