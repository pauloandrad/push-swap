/* =============================================================================
 * simple.c — estratégia O(n²)
 *
 * O QUE FAZER AQUI:
 * - Escolher UMA abordagem simples (ex: selection sort adaptado, insertion
 *   sort adaptado, ou extração sucessiva de mínimo/máximo) e implementá-la
 *   gerando SOMENTE operações da linguagem push_swap (não mexer em arrays
 *   auxiliares — tudo via sa/sb/pa/pb/ra/rb/rra/rrb/ss/rr/rrr).
 * - void run_simple(t_ps *ps): função de entrada chamada pelo selector.
 * - Documentar (aqui em comentário, e depois no README) por que essa
 *   técnica é O(n²) no MODELO de operações do push_swap (não big-O de
 *   array clássico).
 * - Testar isoladamente com --simple antes de integrar ao adaptive.
 * ========================================================================== */
