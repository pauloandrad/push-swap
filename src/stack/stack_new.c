/* =============================================================================
 * stack_new.c
 *
 * O QUE FAZER AQUI:
 * - Função para criar um novo nó (t_node *node_new(int value)) alocando
 *   com malloc e checando falha (chamar erro/exit se malloc falhar).
 * - Função para construir a stack a inteira a partir do array de valores
 *   já parseados/validados (ex: t_node *build_stack(int *values, int n)),
 *   respeitando a ordem: primeiro argumento = topo da stack.
 * - Função para inicializar a struct de estado geral (t_ps): stack a
 *   populada, stack b vazia (NULL), tamanhos, contadores zerados.
 * - Função para liberar toda a stack (free de cada nó) usada no fim do
 *   programa e em qualquer saída de erro.
 * ========================================================================== */
